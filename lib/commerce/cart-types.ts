export type CartItem = {
  slug: string;
  productSlug?: string;
  variantSize?: string;
  variantPrice?: string;
  chemicalName: string;
  catalogNumber: string;
  categorySlug: string;
  shortDescription: string;
  purity: string;
  availability: string;
  quantity: number;
};
